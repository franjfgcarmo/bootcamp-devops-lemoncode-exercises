apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ss-db-todo-app
spec:
  selector:
    matchLabels:
      app: ss-db-todo-app
  serviceName: ss-db-todo-app
  replicas: 1
  template:
    metadata:
      labels:
        app: ss-db-todo-app
    spec:
      containers:
      - name: postgres
        image: postgres:16
        envFrom:
          - configMapRef:
              name: cm-db-todo-app
        ports:
        - containerPort: 5432
          name: postgres      
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: postgres-data
    spec:
      storageClassName: scn-db-todo-app
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 10Gi
